import {
    createIconSetFromIcoMoon
} from 'react-native-vector-icons';

import selectionConfig from './icons/selection.json';

const Icon = createIconSetFromIcoMoon(selectionConfig, 'icomoon');

export default Icon